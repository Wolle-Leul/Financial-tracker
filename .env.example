# Public base URL of the FastAPI backend (no trailing slash).
# Leave empty during local dev to use the Vite proxy to http://127.0.0.1:8000.
# Production example: https://api.example.com
VITE_API_BASE_URL=
